/*The process of combining more than one string together is known as
 * Concatenation.*/

#include <bits/stdc++.h>
using namespace std;

int main() {
  string s = "Find the length of this string";
  int len = s.length(); // storing the lenght of string in 'len'

  cout << "String: " << s << '\n';
  cout << "Length of the string = " << len << '\n';

  return 0;
}
